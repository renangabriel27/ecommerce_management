
<section>
  <table class="ui compact celled definition table">
    <thead class="full-width">
      <tr>
        <th>Id</th>
        <th>Cliente</th>
        <th>Quantidade de produtos</th>
        <th>Total</th>
        <th>Cadastrado em</th>
        <th>Opções</th>
      </tr>
    </thead>
    <tbody>
      <?php
      foreach ($this->orders as $order): ?>
      <tr>
        <td><?= $order->getId() ?></td>
        <td><?= $order->getClientId()->getName() ?></td>
        <td>0</td>
        <td><?= $order->getTotal() ?></td>
        <td><?= $order->getCreatedAt() ?></td>
        <td>
          <?= ViewHelpers::linkTo("/pedidos/{$order->getId()}", '', '', 'class="zoom icon"') ?>
          <?= ViewHelpers::linkTo("/pedidos/{$order->getId()}", '', '', 'class="edit icon"') ?>
          <?= ViewHelpers::linkTo("/pedidos/{$order->getId()}/deletar", '', '', 'class="trash icon"') ?>
        </td>
      </tr>
      <?php endForeach; ?>
    </tbody>
    <tfoot class="full-width">
      <tr>
        <th colspan="8">
            <i class="add icon"></i> <?= ViewHelpers::linkTo("/pedidos/novo", 'Novo pedido') ?>
        </th>
      </tr>
    </tfoot>
  </table>
</section>
