<section>
  <header>
    <h2>Novo pedido</h2>
  </header>
  <article>
    <form action="<?= $this->action ?>" method="POST">

      <div class="form-group <?= $this->order->getErrors('client_id') ? 'has-error' : ' '?>">
        <label for="autocomplete_clients">Cliente</label>
        <input type="text" class="form-control autocomplete-suggestions" id="autocomplete_clients"
          name="order[client_name]" placeholder="Digite o nome do cliente" value=""
          data-url="<?= ViewHelpers::urlFor('/clientes/autocomplete-search') ?>">

        <input type="hidden" class="form-control" id="order_user_id" name="order[client_id]">

        <p class="help-block"><?= $this->order->getErrors('client_id') ?></p>
      </div>

      <button type="submit" name="button" class="btn btn-primary"><?= $this->submit ?> </button>
    </form>

  </article>
</section>
